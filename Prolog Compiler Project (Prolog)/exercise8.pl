/* The following programs use recursion. */

/* a(X):- b(X,Y), a(X). */

/* has_flu(rebecca). */
/* has_flu(john). */
/* has_flu(X):-kisses(X,Y),has_flu(Y) */
/* kisses(janet,john). */

/* ????? */
/* search(end). */
/* search(X):-path(X,Y),search(Y). */
